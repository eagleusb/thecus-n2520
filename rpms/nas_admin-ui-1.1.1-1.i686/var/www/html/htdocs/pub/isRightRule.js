<!--
function isRightRule(s){

           var s = typeof(s.value)=='undefined' ? s : s.value;

           for( idx = 0 ; idx <s.length ; idx++ )

           {

           if( !( ( s.charAt(idx)>= 'a' && s.charAt(idx) <= 'z' ) || ( s.charAt(idx)>= '0' && s.charAt(idx) <= '9' ) || ( s.charAt(idx)>= 'A' && s.charAt(idx) <= 'Z' ) ) )

           return false;

           }

           return true;

           }


	-->
