<!--
    function isNumber(s){
		var s = typeof(s.value) != 'undefined' ? s.value : s;
		for(j=0;j<s.length;j++){
		    tmp = s.charCodeAt(j);
			if(tmp <= 47 || tmp >= 58)
				return false;
		}
		return true;
    }
-->
