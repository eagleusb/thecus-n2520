#!/usr/bin/env node

module.exports = function(cmd, args) {
    var path = require('path');
    args = args || [];
    var fork = require('child_process').fork;
    args.unshift(cmd);
    fork(path.join(process.env.OLDPWD, 'nohup/nohup.js'), args);
}

